# Personal portfolio

My React portfolio for selected projects, work experience, engineering notes, and the stack I use.

## Run locally

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
npm run preview
```

The site is deployed on Netlify. SPA routes are handled by `public/_redirects`.
